<h1>Tasks</h1>
<% @tasks.each do |task| %>
  <div class="task">
    <%= render 'icon', checked: task.completed %>
    <h2><%= link_to task.title, task_path(task) %></h2>
  </div>
<% end %>

<p><%= link_to 'Add a new task', new_task_path %></p>
